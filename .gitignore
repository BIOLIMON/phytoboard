# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Environment — NEVER commit real credentials
.env
.env.local
.env.*.local

# Claude Code — contains shell history with API keys
.claude/

# Supabase CLI temp files — contain project references
supabase/.temp/

# Binary uploads — manage via CDN or git-lfs if needed
uploads/

# Design prototypes — contain hardcoded credentials, not part of the build
_prototypes/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
